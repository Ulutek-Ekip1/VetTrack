/* 
import 'package:vettrack_frontend/features/pet/domain/repositories/pet_repository.dart';

class GetPetVisitsUseCase {
  final PetRepository repository;

  GetPetVisitsUseCase(this.repository);

  Future<List<VisitDetailEntity>> call(String petId) async {
    return await repository.getPetVisits(petId);
  }
} */
